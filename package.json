{
  "name": "erp-crm-system",
  "version": "1.0.0",
  "private": true,
  "description": "DistribuERP - Fullstack Enterprise ERP & CRM on Vercel Serverless",
  "scripts": {
    "postinstall": "prisma generate --schema=./server/prisma/schema.prisma",
    "build": "prisma generate --schema=./server/prisma/schema.prisma && cd client && npm install && npm run build",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "seed:logins": "cd server && npm run seed:logins"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.22.0",
    "socket.io": "^4.8.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^22.9.0",
    "@types/socket.io": "^3.0.1",
    "typescript": "^5.6.3"
  }
}
